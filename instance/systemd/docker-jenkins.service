[Unit]
Description=Jenkins container
Requires=setup-instance.service
After=setup-instance.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start --attach jenkins
ExecStop=/usr/bin/docker stop jenkins